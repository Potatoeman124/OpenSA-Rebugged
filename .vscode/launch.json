{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "OpenSA",
            "type": "node-terminal",
            "request": "launch",
            "preLaunchTask": "OpenSA: Build",
            "command": "cmd.exe /d /c launch-game.cmd",
            "cwd": "${workspaceFolder}"
        }
    ]
}
